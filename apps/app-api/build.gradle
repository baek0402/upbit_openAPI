dependencies {
    implementation project(':protocol')
    implementation project(':libs:application')

    implementation 'org.springframework.boot:spring-boot-starter-web'

    runtimeOnly(project(":libs:persistence"))
    runtimeOnly(project(":libs:http"))
}
