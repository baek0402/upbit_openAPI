spring:
  datasource:
    url: jdbc:mysql://localhost:3306/upbitapi?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: rkddkwl123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    #    hibernate:
    #      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
